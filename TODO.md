# 錦鯉シミュレーション - 実装予定

## 完了済み実装: 餌やり・成長システム (2026/4/11)

### 実装内容の概要
- **餌やりシステム**: 餌の購入・投与・成長促進の完全実装
- **成長システム**: 稚魚→若魚→成魚の3段階成長と視覚的表現
- **UI改善**: 通知最適化、ボタン配置、餌残数表示の強化

### 具体的な実装項目

#### 1. 餌システム基盤
- [x] 餌クラス (`FoodPellet`) の実装（沈むアニメーション、寿命管理）
- [x] 餌インベントリ管理（購入・消費・表示）
- [x] 餌投与のインタラクション（池内クリックで投げる）
- [x] 餌価格設定（30個30万円）と購入ボタン

#### 2. 鯉の餌反応システム
- [x] 餌探知ロジック（視界範囲200px）
- [x] 積極的な追跡行動（速度2.5倍、角度調整強化）
- [x] 餌食べ中状態管理（`isFeeding`状態）
- [x] 群がり行動の最適化（ランダム方向転換抑制）

#### 3. 成長システム
- [x] 3段階成長ステージ（稚魚・若魚・成魚）
- [x] 成長進捗管理（餌1個で20%増加、5個で成魚）
- [x] ステージ遷移時の評価再計算（稚魚30%、若魚60%、成魚100%）
- [x] 視覚的成長表現（サイズ差、模様透明度）

#### 4. UI/UX改善
- [x] 餌購入ボタンのHUD内移動と価格表示
- [x] 餌残数表示の `updateHud()` 統合
- [x] 通知システムの最適化（重複防止、簡略化）
- [x] 浮遊テキスト位置の調整（重なり防止）

#### 5. ゲームバランス調整
- [x] 成魚のみ交配可能制限
- [x] 稚魚・若魚の交配防止ロジック
- [x] 餌投げ通知の削除（池にかぶる問題解決）
- [x] 成長テキストの最適化（成魚時のみ表示）

### 技術的実装詳細
- **餌探知**: `nearestDist < 200`、`turnStrength = 0.4 + 0.3 * foodInterest`
- **速度増加**: `targetSpeed = this.baseSpeed * (1.0 + 1.5 * foodInterest)`
- **成長進捗**: `this.growthProgress = Math.min(1.0, this.growthProgress + 0.2)`
- **サイズ調整**: 稚魚40%、若魚70%、成魚100% (`displaySize`)
- **模様透明度**: 稚魚30%、若魚70%、成魚100% (`patternAlpha`)

---

## 次回実装予定: 収益システムの拡張

### 提案内容
現在の収益システムは放置収入のみですが、以下を追加提案します：
1. **餌販売**: 餌を投げることで観客が増え、収入アップ
2. **成長展示**: 成魚になった鯉を特別展示して収入増加
3. **品種別収益**: 希少品種ほど高い収益を生む

### 検討事項
- ゲームバランスへの影響
- UI追加の必要性
- 実装コスト vs プレイ体験の向上

---

## 完了済み実装: 変異システムの整理と新しい色の追加

### 問題の詳細な説明
現在の変異システムには以下の問題がある：
1. **優先順位ベースの競合**: `specialMutation`フラグを使用しており、最初に発動したルールが他のルールをブロックする
2. **ルールの依存関係が複雑**: ルールが増えるにつれて、どのルールがどのルールを阻害するかが不明確
3. **確率の重複と競合**: 複数のルールが同じ条件を満たす場合、最初のルールのみが発動する
4. **新しい色の追加が困難**: 新しいルールを追加すると、既存のルールの発動確率が低下する
5. **デバッグとバランス調整が困難**: ルールが11個もあり、相互関係を理解するのが難しい

### 具体的な解決策
**確率の重み付けシステム**を導入する：
1. 条件を満たすすべてのルールを収集
2. 各ルールに重み（確率）を設定
3. 重みに基づいてランダムに1つのルールを選択
4. 選択されたルールを適用

### 実装タスクのリスト
- [x] 現在の変異ルールを分析し、ルール定義を抽出（11個のルールを分析）
- [x] 重み付けシステムの設計と実装（重みに基づく確率選択システム）
- [x] 既存の11個の変異ルールを新しいシステムに移行（すべてのルールをオブジェクト化）
- [x] 新しいシステムの動作確認とバランス調整（コードレベルの整合性確認完了）
- [x] 新しい色の追加のための基盤を整備（モジュール化されたルールシステム）

### 次の実装フェーズ：ピンク色と銀色の追加（実装完了）
- [x] 色定義の追加（ピンク色 `#ff66b2`、銀色 `#c0c0c0`）
- [x] 変異ルールの実装（白＋赤→ピンク、ピンク→銀など）
- [x] 品種定義の追加（KOI_BREEDS配列に新しい品種）
- [x] 品種判定ロジックの更新（analyzeBreed関数）
- [x] ドキュメントの更新（MUTATION_SYSTEM.md）
- [x] テストとバランス調整（ユーザーが実施）

### 期待される効果
1. **新しいルールの追加が容易**: 既存のルールの相対的な確率を維持しながら新しいルールを追加可能
2. **バランス調整が容易**: ルールの重みを調整するだけで確率を簡単に変更可能
3. **デバッグが容易**: どのルールが発動したか追跡しやすくなる
4. **新しい色の追加の準備**: システムが整理され、新しい色（例：緑色、橙色など）の追加が容易になる

### 注意事項
- 既存の遺伝ロジックを尊重する
- ゲームバランスを維持する
- 後方互換性を確保する（既存のゲームデータが壊れないように）